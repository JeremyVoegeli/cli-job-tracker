[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cli-job-tracker"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "click>=8.0",
]

[project.scripts]
jobtracker = "jobs.cli:cli"

[tool.pytest.ini_options]
testpaths = ["tests"]
